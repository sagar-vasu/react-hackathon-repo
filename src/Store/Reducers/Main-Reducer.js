const initialState = {
    home: true,
}



const MainReducer = (state = initialState, action) => {

    // switch (action.type) {
    //     case "ALREADY_USER":
    //         console.log(action.payload, 'hi iam from main')
    //         return state

    //     default: return state

    // }

    return state


}

export default MainReducer